const authApiRequests = {
    login: (email: string, password: string) => console.log(email, password),
    register: (email: string, password: string) => console.log(email, password),
}

export default authApiRequests;